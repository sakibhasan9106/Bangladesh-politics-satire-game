# FIX_MANIFESTO.md — লোডশেডিং গেম, নির্দিষ্ট ৭টা বাগ ফিক্স

> **এই ডকুমেন্টটা অন্য Claude সেশনে/অ্যাকাউন্টে পেস্ট করে ব্যবহারের জন্য।**
> নিচে ৭টা আলাদা সমস্যা লিস্ট করা আছে, প্রতিটার সাথে বর্তমান কোডে ঠিক কোথায় (ফাইল + লাইন-সংখ্যা কাছাকাছি, exact string) সমস্যাটা আছে সেটাও দেওয়া আছে।

## ⚠️ সবচেয়ে গুরুত্বপূর্ণ নিয়ম (যেভাবেই ফিক্স করা হোক)
- **শুধু নিচের ৭টা নির্দিষ্ট জিনিস ঠিক করতে হবে। আর কিছু বদলানো যাবে না।**
- Landscape/rotation লজিক (iOS + Android), touch/tap remap, score/difficulty/collision লজিক, sound, existing Bengali-font-padding ফিক্স (`BENGALI_FONT`, `BENGALI_TEXT_PADDING` কনস্ট্যান্ট) — এগুলার কোনোটাই টাচ করা যাবে না, এইগুলা আগেই আলাদাভাবে টেস্ট করে ফিক্স করা হয়েছে।
- প্রতিটা ফিক্সের পর `npm run build` চালিয়ে কনফার্ম করতে হবে কোনো নতুন error/warning আসেনি।
- একটার বেশি ইস্যু একসাথে না ধরে, একটা একটা করে ফিক্স করে প্রতিটার পর বিল্ড টেস্ট করাই নিরাপদ।

---

## ইস্যু ১ — "\u2190/\u2192" লেখাটা বাদ দিতে হবে

**সমস্যা:** ইন-গেম ইন্সট্রাকশন টেক্সটে আসলে তীর-চিহ্ন (← →) দেখানোর কথা ছিল, কিন্তু কোডে ভুল escaping-এর কারণে স্ক্রিনে হুবহু `\u2190/\u2192` (ব্যাকস্ল্যাশ-u-সংখ্যা আকারে) লেখাটাই দেখা যাচ্ছে।

**কোথায় (`src/main.js`, `MainScene.create()`-এর ভেতরে, `instructionText`):**
```js
this.instructionText = this.add
  .text(
    GAME_WIDTH / 2,
    20,
    'বামে/ডানে ট্যাপ করো (বা \\u2190/\\u2192 কী) লেন পাল্টাতে',
    {
      fontFamily: BENGALI_FONT,
      fontSize: '16px',
      color: '#000000',
      padding: BENGALI_TEXT_PADDING,
    }
  )
  .setOrigin(0.5, 0);
```

**কী করতে হবে:** স্ট্রিং থেকে `(বা \\u2190/\\u2192 কী)` অংশটা পুরোপুরি বাদ দিয়ে দিতে হবে (মোবাইলে তো কীবোর্ড arrow key ব্যবহারই হয় না, শুধু ট্যাপ দিয়েই চলে — তাই এই অংশটা আসলে দরকারও নাই)। ফলাফল হওয়া উচিত:
```js
'বামে/ডানে ট্যাপ করো লেন পাল্টাতে'
```
বাকি স্টাইল (`fontFamily`, `fontSize`, `color`, `padding`) অপরিবর্তিত রাখতে হবে।

---

## ইস্যু ২ — "A Political Satire Game" (ইংরেজিতে, বোল্ড) যোগ করতে হবে

**সমস্যা:** টাইটেল স্ক্রিনে শুধু বাংলা সাবটাইটেল আছে ("একটা লোডশেডিং স্যাটায়ার গেম"), ইউজার চান এর পাশাপাশি/নিচে ইংরেজিতে বোল্ড করে "A Political Satire Game" জাতীয় একটা লাইন থাকুক (og:description-এও একই ভাব বাংলায় আছে, কিন্তু on-screen ইংরেজি টেক্সট নাই)।

**কোথায় (`src/main.js`, `TitleScene.create()`-এর ভেতরে, সাবটাইটেলের ঠিক পরে):**
```js
// ---- সাবটাইটেল ----
this.add
  .text(GAME_WIDTH / 2, 250, 'একটা লোডশেডিং স্যাটায়ার গেম', {
    fontFamily: BENGALI_FONT,
    fontSize: '18px',
    color: '#cccccc',
    padding: BENGALI_TEXT_PADDING,
  })
  .setOrigin(0.5);
```

**কী করতে হবে:** এই ব্লকের ঠিক পরে (কিন্তু "Tap / Space চাপো শুরু করতে" টেক্সটের আগে) একটা নতুন ইংরেজি, বোল্ড টেক্সট যোগ করতে হবে, যেমন:
```js
this.add
  .text(GAME_WIDTH / 2, 278, 'A POLITICAL SATIRE GAME', {
    fontFamily: 'Arial, sans-serif', // বাংলা ফন্ট এখানে লাগবে না, এটা pure ইংরেজি
    fontSize: '14px',
    color: '#999999',
    fontStyle: 'bold',
    letterSpacing: 2, // যদি Phaser ভার্সন সাপোর্ট করে; না করলে বাদ দেওয়া যাবে
  })
  .setOrigin(0.5);
```
Y-পজিশন (278) এমনভাবে বসাতে হবে যেন বাংলা সাবটাইটেল (y=250) আর "Tap/Space" টেক্সটের (y=340) মাঝে ওভারল্যাপ না করে জায়গা মতো বসে — দরকার হলে দুটোর Y-ভ্যালু সামান্য অ্যাডজাস্ট করা যেতে পারে, কিন্তু অন্য কোনো টেক্সট/এলিমেন্ট সরানো যাবে না।

---

## ইস্যু ৩ — নির্মাতার ক্রেডিট লাইন আরও শার্প/ভিজিবল করতে হবে

**সমস্যা:** "নির্মাতা: Sakib Hasan (আর কেউ দায়ী না)" লেখাটা এখন এত হালকা (low opacity) যে প্রায় দেখাই যায় না।

**কোথায় (`src/main.js`, `MainScene.create()`-এর একদম শেষে):**
```js
this.creditText = this.add
  .text(10, GAME_HEIGHT - 10, 'নির্মাতা: Sakib Hasan (আর কেউ দায়ী না)', {
    fontFamily: BENGALI_FONT,
    fontSize: '11px',
    color: '#ffffff',
    padding: BENGALI_TEXT_PADDING,
  })
  .setOrigin(0, 1)
  .setAlpha(0.55)   // ⚠️ এইটাই মূল কারণ — ৫৫% অপাসিটি
  .setDepth(30)
  .setScrollFactor(0);
```

**কী করতে হবে (দুইটা অপশন, যেকোনো একটা বা দুটোই):**
1. `.setAlpha(0.55)` বাড়িয়ে `.setAlpha(0.85)` বা `.setAlpha(1)` করা।
2. পড়া আরও সহজ করতে একটা হালকা কালো `stroke` যোগ করা, যাতে যেকোনো background-এর ওপরেও কনট্রাস্ট থাকে:
```js
this.creditText = this.add
  .text(10, GAME_HEIGHT - 10, 'নির্মাতা: Sakib Hasan (আর কেউ দায়ী না)', {
    fontFamily: BENGALI_FONT,
    fontSize: '11px',
    color: '#ffffff',
    stroke: '#000000',
    strokeThickness: 3,
    padding: BENGALI_TEXT_PADDING,
  })
  .setOrigin(0, 1)
  .setAlpha(0.9)
  .setDepth(30)
  .setScrollFactor(0);
```
`fontSize` সামান্য বাড়ানো (11px → 12-13px) যেতে পারে যদি আরও ভালোভাবে চোখে পড়াতে চান, কিন্তু জায়গা/লেআউট নষ্ট না করে।

---

## ইস্যু ৪ — বোনাস "+১০%" পপ-আপ টেক্সট স্পষ্ট করতে হবে

**সমস্যা:** বোনাস আইটেম খেলে যে "+১০%" পপ-আপ ভাসে, সেটা প্রায়ই পড়া যায় না।

**আসল কারণ:** টেক্সটের রঙ (`#ff2b2b`, উজ্জ্বল লাল) আর পেছনের blood-splatter ইমেজ — দুটোই কাছাকাছি লাল/মেরুন রঙের, তাই টেক্সট পেছনের সাথে মিশে যায় (কম কনট্রাস্ট), সাথে এটা ৫০০ms-এই দ্রুত ভেসে fade-out হয়ে যায়।

**কোথায় (`src/main.js`, `eatBonus()` মেথডের ভেতরে):**
```js
const popup = this.add
  .text(mouthX, mouthY - 10, `+${BONUS_SCORE_VALUE}%`, {
    fontSize: '22px',
    color: '#ff2b2b',
    fontStyle: 'bold',
    stroke: '#5c0a0a',
    strokeThickness: 4,
    shadow: {
      offsetX: 0,
      offsetY: 2,
      color: '#3a0505',
      blur: 3,
      fill: true,
    },
  })
  .setOrigin(0.5)
  .setDepth(15);
```

**কী করতে হবে:** টেক্সটের রঙ পাল্টে এমন কিছু করতে হবে যেটা লাল/মেরুন splatter-এর ওপর হাই-কনট্রাস্ট দেখায় — যেমন উজ্জ্বল সোনালি/হলুদ (`#ffd700` বা `#ffe066`) + গাঢ় কালো/মেরুন stroke:
```js
const popup = this.add
  .text(mouthX, mouthY - 10, `+${BONUS_SCORE_VALUE}%`, {
    fontSize: '24px',
    color: '#ffe066',       // লাল splatter-এর ওপর হাই-কনট্রাস্ট সোনালি/হলুদ
    fontStyle: 'bold',
    stroke: '#3a0505',
    strokeThickness: 5,
    shadow: {
      offsetX: 0,
      offsetY: 2,
      color: '#000000',
      blur: 4,
      fill: true,
    },
  })
  .setOrigin(0.5)
  .setDepth(15);
```
চাইলে fade-out duration (`this.tweens.add({ ... duration: 500 ... })`, এই মেথডেরই একটু নিচে) সামান্য বাড়িয়ে (যেমন 500 → 650ms) পড়ার জন্য একটু বেশি সময় দেওয়া যায় — কিন্তু এটা optional, মূল ফিক্স হলো রঙ পাল্টানো।

---

## ইস্যু ৫ — চাঁদাবাজ/ছিনতাইকারি/সন্ত্রাসী বোনাস আইকন + বাংলা লেবেল শার্প করতে হবে

**সমস্যা:** এই ৩টা বোনাস আইটেমের ছবি (আইকন) লো-কোয়ালিটি/অস্পষ্ট দেখাচ্ছে, আর তাদের নিচের বাংলা লেবেল টেক্সটও শার্প না।

**আসল কারণ (ছবির দিক থেকে):** `PROJECT_STATUS.md`-এ লেখা আছে এই ৩টা PNG একটা single JPEG স্প্রাইট-শিট থেকে flood-fill দিয়ে ক্রপ করে বের করা হয়েছিল। বর্তমান রেজোলিউশন:
- `bonus_chadabaj.png` — 95×140px
- `bonus_chintaikari.png` — 100×140px
- `bonus_shontrashi.png` — 97×140px

JPEG কম্প্রেশনের কারণে কিনারায় (edge) ব্লার/আর্টিফ্যাক্ট থাকে, আর রেজোলিউশনও তুলনামূলক কম — canvas-এ বড় করে (perspective scale-এ কাছে আসলে) দেখানোর সময় এই ব্লার আরও প্রকট হয়।

**আসল কারণ (টেক্সটের দিক থেকে, শুধু এই লেবেলের না — পুরো গেমের সব টেক্সটেরই একটা সাধারণ কারণ):** Phaser গেম কনফিগে (`src/main.js`-এর একদম নিচে, `const config = { ... }`) কোনো `resolution`/devicePixelRatio সেটিং নাই। মানে canvas-এর ভেতরের আসল রেন্ডার-রেজোলিউশন সবসময় 800×450 ফিক্সড থাকে, কিন্তু ফোনের হাই-DPI স্ক্রিনে (Android/iOS-এ প্রায় সবগুলাতেই devicePixelRatio 2-3) সেটাকে CSS দিয়ে বড় করে দেখানো হয় — ফলে ভেতরের প্রতিটা টেক্সট/ছবি একটু ব্লার দেখায়, বিশেষ করে ছোট ফন্টের ক্ষেত্রে (যেমন `fontSize: '10px'` বোনাস লেবেলে) এটা বেশি চোখে পড়ে।

**কী করতে হবে:**

1. **টেক্সট শার্পনেস (কোড-লেভেল ফিক্স, তুলনামূলক সহজ):** `config` অবজেক্টে `resolution` যোগ করা —
```js
const config = {
  type: Phaser.AUTO,
  width: GAME_WIDTH,
  height: GAME_HEIGHT,
  parent: 'app',
  scene: [TitleScene, MainScene],
  resolution: window.devicePixelRatio || 1,   // ⚠️ নতুন লাইন — এটাই মূল ফিক্স
  scale: {
    mode: Phaser.Scale.NONE,
    width: GAME_WIDTH,
    height: GAME_HEIGHT,
  },
};
```
   ⚠️ সতর্কতা: এই একটা লাইন পাল্টালে canvas-এর ভেতরের actual pixel resolution বেড়ে যাবে (retina স্ক্রিনে ২-৩ গুণ), যেটা performance-এ সামান্য প্রভাব ফেলতে পারে লো-এন্ড ফোনে — তাই যোগ করার পর ফোনে (বিশেষ করে পুরনো/সস্তা Android ফোনে) FPS/স্মুথনেস ঠিক আছে কিনা টেস্ট করে দেখা উচিত। Landscape rotation/layoutCanvas() লজিকের সাথে এটা conflict করার কথা না (ওটা CSS size নিয়ে কাজ করে, এটা internal render resolution নিয়ে), কিন্তু যেহেতু এটা touch করা হচ্ছে না বলে আগে বলা হয়েছিল, তাই যোগ করার পর অবশ্যই আবার Android + iOS দুই জায়গাতেই lane-switch tap আর landscape rotation টেস্ট করে কনফার্ম করতে হবে কিছু ভেঙে যায়নি।

2. **ছবির শার্পনেস (art-level ফিক্স, কোড দিয়ে সমাধান হবে না):** এই ৩টা PNG-র (`src/assets/art/png/bonus_chadabaj.png`, `bonus_chintaikari.png`, `bonus_shontrashi.png`) কোয়ালিটি বাড়ানোর জন্য আসল সোর্স ছবিই বদলাতে হবে, কোড দিয়ে ব্লার সরানো সম্ভব না। দুইটা পথ:
   - **(ক)** ইউজার (আপনি) নতুন, বেশি রেজোলিউশনের, পরিষ্কার transparent-background PNG বানিয়ে/জেনারেট করে দিলে, একই ফাইলনেম/সাইজ-অনুপাত রেখে `src/assets/art/png/` ফোল্ডারে replace করলেই চলবে — কোনো কোড বদলাতে হবে না (import path/texture key অপরিবর্তিত থাকবে)।
   - **(খ)** অথবা আগের মতো hand-coded flat-vector SVG দিয়ে এই ৩টা আইকন আবার বানানো (আগের `Iteration 7`-এর SVG স্টাইলে) — vector আর্ট হওয়ায় যত বড়ই দেখানো হোক, কখনো ব্লার হবে না। এই কাজটা AI (আমি/অন্য Claude সেশন) নিজে থেকেও করতে পারবে, নতুন ইমেজ আপলোডের দরকার নাই।
   - ⚠️ **আপনি যা বলেছেন সেটাই সঠিক পথ:** "best possible solution" মানে যদি নতুন হাই-রেজ PNG বানিয়ে দিতে পারেন সেটাই সবচেয়ে ভালো ফল দেবে (আসল আর্ট স্টাইল বজায় থাকবে), অথবা ভেক্টর SVG দিয়ে রিমেক করাটা দ্বিতীয় সেরা অপশন (গ্যারান্টিড শার্প, কিন্তু flat-cartoon স্টাইলে চলে যাবে, বর্তমান painterly PNG স্টাইলের থেকে ভিন্ন লুক হবে)।

3. **লেবেল টেক্সট (`type.name`) ছোট সাইজ ইস্যু:** `fontSize: '10px'` বেশ ছোট — resolution ফিক্স (উপরে #1) প্রয়োগের পরও চাইলে `12px`-এ বাড়ানো যেতে পারে (`src/main.js`-এ `spawnBonus()` মেথডে) পড়ার সুবিধার জন্য, তবে এটা optional।

---

## ইস্যু ৬ — গেমের সার্বিক কনট্রাস্ট বাড়াতে হবে

**সমস্যা:** পুরো গেমের ভিজ্যুয়াল কনট্রাস্ট কম লাগছে (ছবি/টেক্সট/HUD সব মিলিয়ে "flat"/washed-out লাগছে)।

**সম্ভাব্য কারণ ও ফিক্স-অপশন (এইটা ছোট এক-লাইন ফিক্স না, তাই কয়েকটা অপশন দেওয়া হলো — যেটা সবচেয়ে কম রিস্কি মনে হয় সেটা আগে ট্রাই করা ভালো):**

1. **সবচেয়ে নিরাপদ/সহজ — CSS filter দিয়ে পুরো canvas-এর কনট্রাস্ট বাড়ানো** (`src/style.css`, `#app canvas` rule-এ):
```css
#app canvas {
  display: block;
  filter: contrast(1.15) saturate(1.1); /* নতুন লাইন — সংখ্যা টিউন করা যাবে */
}
```
   এটা কোনো game logic টাচ করে না, শুধু ভিজ্যুয়াল আউটপুটে একটা পোস্ট-প্রসেসিং ফিল্টার বসায় — সবচেয়ে কম রিস্ক। সংখ্যা (1.15, 1.1) কম/বেশি করে চোখে যতটুকু ভালো লাগে ততটুকু টিউন করা যায়।

2. **আরেকটু বেশি নিয়ন্ত্রণ চাইলে — Phaser camera-তে contrast/color pipeline effect** (`MainScene`-এ, `create()`-এর শুরুতে ক্যামেরা সেটআপের কাছে) — এটা কোড-লেভেলে বেশি জটিল, শুধু দরকার হলেই এই পথে যাওয়া উচিত।

3. **নির্দিষ্ট এলিমেন্টের কনট্রাস্ট আলাদাভাবে বাড়ানো** — যেমন HUD টেক্সট (`Level:`, `লুটপাট:`)-এ এখন `color: '#000000'` (কালো, কোনো stroke নাই) — background-এর সাথে মিশে যাচ্ছে কিনা দেখে দরকার হলে `stroke: '#ffffff', strokeThickness: 2` যোগ করা যায়, ইস্যু #৩-এর মতোই পদ্ধতিতে।

⚠️ যেহেতু "কনট্রাস্ট কম" একটা subjective/সার্বিক feedback (নির্দিষ্ট এক লাইনের বাগ না), তাই #১ (CSS filter) দিয়ে শুরু করে ব্রাউজারে দেখে, তারপর দরকার হলে সংখ্যা বাড়ানো/কমানো — এটাই সবচেয়ে কম রিস্কি, iterative পথ।

---

## ইস্যু ৭ — ব্যাকগ্রাউন্ডের "বিদ্যুৎ নাই", "জিনিসের দাম কমাও", কনসার্ট-সংক্রান্ত টেক্সট ব্লারি

**সমস্যা:** `street_background.png`-এর ভেতরে আঁকা প্রতিবাদী প্ল্যাকার্ড/সাইনবোর্ডের বাংলা টেক্সট ("বিদ্যুৎ নাই", "জিনিসের দাম কমাও") আর কনসার্ট-সংক্রান্ত অংশটা ঝাপসা/ব্লারি দেখাচ্ছে।

**আসল কারণ:** এই ব্যাকগ্রাউন্ড ছবিটা মূলত Gemini দিয়ে জেনারেট করা JPEG (`edited6.jpeg`) থেকে PNG-তে কনভার্ট করা (`src/assets/art/png/street_background.png`, বর্তমানে 1376×768px, কিন্তু pixel-mode হলো "8-bit colormap" অর্থাৎ ইনডেক্সড পালেট — এটা মূল JPEG-এর কম্প্রেশন-ব্লার + PNG কনভার্শনের সময় পালেট-বেসড কালার রিডাকশন, দুটোই মিলিয়ে ছোট/ডিটেইলড টেক্সট এরিয়ায় ঝাপসা ভাব তৈরি করে)। এটা মূলত **সোর্স ইমেজ কোয়ালিটির সমস্যা, কোড দিয়ে ঠিক করা সম্ভব না।**

**কী করতে হবে — আপনি নিজেই যা বলেছেন সেটাই সঠিক:**
- **নতুন, বেশি রেজোলিউশনের, PNG (indexed-palette না, সরাসরি true-color/24-bit PNG বা কম-কম্প্রেশন JPEG) ব্যাকগ্রাউন্ড বানিয়ে/জেনারেট করে দিলে**, শুধু `src/assets/art/png/street_background.png` ফাইলটা replace করে দিলেই হবে — `main.js`-এ কোনো কোড বদলানোর দরকার নাই (import path/texture key/`BG_ZOOM` সবকিছু অপরিবর্তিত থাকবে), যদি নতুন ছবির aspect ratio পুরনোটার (≈1.79:1, widescreen 16:9-এর কাছাকাছি) মতোই থাকে।
- **ছবি জেনারেট করার সময় এই দুইটা জিনিস খেয়াল রাখা জরুরি:**
  1. Aspect ratio ≈ 16:9 (canvas 800×450 = 1.78:1-এর কাছাকাছি) — নাহলে আগের মতো crop হয়ে সাইনবোর্ড/টেক্সট কেটে যেতে পারে (এই সমস্যাটা আগে একবার হয়েছিল, `PROJECT_STATUS.md`-এর "Background আপডেট #২/#৩" সেকশনে বিস্তারিত আছে)।
  2. এক্সপোর্ট করার সময় PNG-কে যেন **সরাসরি source থেকে** (JPEG-এ কনভার্ট না করে, বা করলেও উচ্চ-কোয়ালিটি সেটিং-এ) সেভ করা হয় — টেক্সট/সাইনবোর্ড এরিয়াগুলা বিশেষভাবে জুম করে চেক করে নেওয়া ভালো যে ওগুলো readable/sharp আছে।
- এই ফাইলটা প্রতিস্থাপনের পর `npm run build` চালিয়ে confirm করতে হবে নতুন asset আলাদা ফাইল হিসেবে `dist/assets/`-এ বসেছে (inline-base64 না হয়ে) — এই প্রজেক্টে আগে থেকেই সব ইমেজ import-এ `?no-inline` সাফিক্স ব্যবহার করা আছে, তাই এইটা এমনিতেই ঠিকভাবে কাজ করার কথা, নতুন করে কিছু যোগ করার দরকার নাই।

---

## সারসংক্ষেপ — কোনটা pure কোড-ফিক্স, কোনটাতে নতুন আর্ট/ছবি লাগবে

| # | ইস্যু | শুধু কোড দিয়ে ফিক্স হবে? |
|---|-------|---------------------------|
| ১ | `\u2190/\u2192` টেক্সট বাদ | ✅ হ্যাঁ (১ লাইন স্ট্রিং এডিট) |
| ২ | "A Political Satire Game" যোগ | ✅ হ্যাঁ (নতুন টেক্সট অবজেক্ট) |
| ৩ | ক্রেডিট লাইন শার্প করা | ✅ হ্যাঁ (alpha/stroke) |
| ৪ | "+১০%" পপ-আপ কনট্রাস্ট | ✅ হ্যাঁ (রঙ বদলানো) |
| ৫ | বোনাস আইকন শার্পনেস | ⚠️ আংশিক — টেক্সট অংশ কোড দিয়ে (resolution ফিক্স), **ছবির আসল কোয়ালিটি নতুন PNG/SVG লাগবে** |
| ৬ | সার্বিক কনট্রাস্ট | ✅ হ্যাঁ (CSS filter, তবে সংখ্যা চোখে দেখে টিউন করা লাগবে) |
| ৭ | ব্যাকগ্রাউন্ড টেক্সট ব্লার | ❌ না — **নতুন হাই-রেজ ব্যাকগ্রাউন্ড ছবি লাগবেই**, কোড দিয়ে সমাধান নাই |

---

*এই ডকুমেন্ট `PROJECT_STATUS.md`-এর বর্তমান কোড-স্টেট (Bengali-font-padding ফিক্স + iOS landscape-rotation ফিক্স-এর পরের ভার্সন) দেখে বানানো হয়েছে। অন্য সেশনে কাজ করানোর আগে ওই দুইটা আগের ফিক্স যেন উল্টে না যায় সেটা নিশ্চিত করতে বলবেন।*
